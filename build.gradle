// Root build.gradle — Android build uses android/build.gradle exclusively.
// This file exists only for IDE compatibility at the repo root level.
// All plugin declarations (Detekt, OWASP, etc.) are in android/build.gradle.

allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
