# Word Pairing: Category Match — .clinerules

## Project Overview

**Name:** Word Pairing: Category Match  
**Type:** React + TypeScript + Capacitor Android app  
**Description:** A sleek, dark-mode word association puzzle game where players group related word tiles to solve categories across multiple game modes.  
**Package:** com.snickerdoodlez.wordpairing  
**Repository:** git@github.com:snickerdoodlez-games/android.git  
**License:** ISC

---

## Tech Stack

- **Framework:** React 19 + TypeScript 5.8
- **Bundler:** Vite 6
- **Mobile:** Capacitor 7 (Android)
- **Animation:** Framer Motion 11.15
- **Styling:** Tailwind CSS (via CDN in index.html)
- **Ads:** @capacitor-community/admob
- **Font:** Oswald (Google Fonts)
- **UI Theme:** Neon arcade color scheme with dark background

---

## Build & Run Commands

| Command | Description |
|---------|-------------|
| `npm install` | Install all dependencies |
| `npm run dev` | Start Vite dev server (port 3000) |
| `npm run build` | Production build to `dist/` |
| `npm run preview` | Preview production build |

---

## Project Structure

```
c:/Users/DD/New folder/gitttt/
├── App.tsx                    # Root React component + game state machine
├── index.tsx                  # Entry point
├── index.html                 # HTML shell with Tailwind CDN + Oswald font
├── types.ts                   # Shared TypeScript types, enums, game constants
├── vite.config.ts             # Vite configuration (aliases, GEMINI_API_KEY)
├── capacitor.config.json      # Capacitor config (outputs to dist/)
├── metadata.json              # AI Studio metadata
├── components/                # React components
│   ├── Level1_Standard.tsx    # Classic / Synonyms mode
│   ├── Level1_Emoji.tsx       # Emoji mode
│   ├── Level2_Filter.tsx      # Filter mode
│   ├── Level5_Group.tsx       # Mind Match mode
│   ├── Level6_Hidden.tsx      # Hidden tiles mode
│   ├── Level7_Expansion.tsx   # Expansion mode (hard)
│   ├── Level7_Expansion_Easy.tsx
│   ├── Level7_Expansion_Medium.tsx
│   ├── Level8_Cascade.tsx     # Cascade mode
│   ├── Level_Themed.tsx       # Themed categories mode
│   ├── LevelLayout.tsx        # Shared level layout wrapper
│   ├── LevelMenu.tsx          # Main menu
│   ├── LevelSuccessScreen.tsx # Level completion overlay (stub)
│   ├── Tile.tsx               # Game tile component
│   ├── Header.tsx             # Level header
│   ├── Footer.tsx             # Ad banner footer
│   ├── SettingsMenu.tsx       # Settings overlay
│   ├── StatsOverlay.tsx       # Player stats / Hall of Fame
│   ├── CategorySelectionOverlay.tsx  # Category pool manager
│   ├── HowToPlay.tsx          # Tutorial screen
│   ├── LeaderboardOverlay.tsx # Leaderboard (stub)
│   ├── ScoreBreakdownOverlay.tsx # Score breakdown (stub)
│   ├── CategoryListOverlay.tsx # Category list (stub)
│   ├── AndroidSystemBars.tsx  # System bar management
│   ├── ArcadeIcon.tsx         # SVG icon component
│   ├── ArcadeStyledText.tsx   # Styled text component
│   ├── LockedAnimationPool.tsx # Locked tile animation
│   ├── NextLevelButton.tsx    # Next level button (stub)
│   ├── SolvedRowBackground.tsx # Solved row background
│   ├── ThemeContext.tsx        # Theme context provider
│   └── WindowSizeClassProvider.tsx # Window size detection
├── services/                  # Data & utility services
│   ├── csvData.ts             # Main CSV data loader
│   ├── csvPoolData1-13.ts     # Pool data files USED IN CLASSIC, EXPANSION, MIND MATCH, HIDDEN, FILTER, GROUP, CASCADE
│   ├── csvThemeData1-9.ts     # Theme data files USED IN THEMED LEVELS
│   ├── csvThemeDataLoader.ts  # Theme data loader
│   ├── CSV_SYNONYMS_1-4.ts    # Synonyms data USED IN SYNONYMS LEVEL
│   ├── csvEasy/Medium/Hard.ts # Difficulty-sorted CSV
│   ├── csvAdd.ts              # CSV addition utilities
│   ├── csvUtils.ts            # CSV parsing utilities
│   ├── levelContent.ts        # Level content generation
│   ├── levelPreCheck.ts       # Level validation
│   ├── storage.ts             # LocalStorage persistence
│   ├── audioService.ts        # Sound effects
│   ├── antonymData.ts         # Antonym data
│   ├── emojiData.ts           # Emoji data USED IN EMOJI LEVEL
│   ├── globalCSV.ts           # Global CSV data
│   ├── masterData.ts          # Master data aggregator
│   ├── newCatData.ts          # New category data
│   ├── rhyme.ts               # Rhyme data
│   ├── synonymData.ts         # Synonym data
│   ├── tileStyles.ts          # Tile styling utilities
│   └── translationData.ts     # Translation data
├── plugins/                   # Native plugins
│   ├── consent.ts             # Consent management
│   ├── privacy.ts             # Privacy options
│   └── windowSizeClass.ts     # Window size class
├── android/                   # Android/Capacitor native project
├── app/                       # App build directory
├── assets/                    # Build assets
├── build-pipeline-mcp-server/ # MCP build pipeline server
├── mcp-server/                # MCP server
├── public/                    # Public static assets
└── src/                       # Source styles
```

---

## Code Conventions

### Imports
```
- React/hooks first
- Types next
- Services and utilities
- Components (static first, lazy-loaded last)
```

### Naming
- **Components:** PascalCase (e.g., `Level1_Standard.tsx`)
- **Services:** camelCase (e.g., `audioService.ts`, `csvUtils.ts`)
- **Types/Interfaces:** PascalCase (e.g., `CSVRow`, `TileData`)
- **Enums:** PascalCase (e.g., `GameMode`)
- **Constants:** UPPER_SNAKE_CASE (e.g., `HEADER_MIN_HEIGHT`)

### Styling
- **Tailwind classes** used via CDN. Do not add custom CSS unless necessary.
- **Neon color palette** is defined in `index.html` tailwind config and `types.ts`.
- Use `bg-neon-*`, `shadow-[0_0_15px_#COLOR,...]` patterns for glowing effects.
- **Always use the existing neon color scheme.** Never introduce new color systems.
- Never change the font (Oswald) without explicit instruction.

### CSV Data Format
```
id,Category Name,"Description",Word1,"Definition1",Word2,"Definition2",...,Difficulty,BroadCategory
```
- Each row: id | name | description | (word + definition) pairs | difficulty | broadCategory
- Words: alternating word-definition pairs after the description
- Difficulty: numeric (1=easy, 3=medium, 5=hard)
- BroadCategory: one of Nature, Animals, Astronomy, Food, History, Geography, Religion, Technology, Science, Music, Literature, Movies, Television, Economics, Politics, Lifestyle, Gaming, Cars, Sports, Art, Mythology

### Game Modes (from GameMode enum)
- `CLASSIC` - Group 4 related word tiles.  Uses the csvPoolData*.ts for game data
- `LEVEL_EMOJI` - Emoji tiles instead of words.  Uses emojiData.ts for game data
- `LEVEL_MIND_MATCH` - Timed matching rounds (20 tiles, 4 per match, 3 rounds, 3 errors).  Uses the csvPoolData*.ts for game data
- `LEVEL_SYNONYMS` - Group by meaning.  Uses the CSV_SYNONYMS_*.ts for game data
- `LEVEL_THEMED` - Themed categories.  Uses the csvThemeData*.ts for game data
- `LEVEL_EXPANSION` / `LEVEL_CASCADE` - Uses the csvPoolData*.ts for game data
- `HIDDEN` - Tiles start hidden, revealed when row solved
- `FILTER` / `GROUP` - Uses the csvPoolData*.ts for game data

---

## Accessibility Requirements

### Touch Target Size
- All interactive elements (buttons, toggles, icon buttons, clickable tiles) MUST have a minimum touch target of 48dp × 48dp.
- Use Tailwind classes `min-w-[48px] min-h-[48px]` on buttons to ensure this.
- Icon-only buttons (close/X, hamburger menu, etc.) must also meet this requirement.
- If a button's visual size is smaller than 48dp, add padding or use `min-w-[48px] min-h-[48px]` with `flex items-center justify-center`.

### Content Description
- All interactive non-TextView elements MUST have a `contentDescription` or `aria-label` attribute for screen reader accessibility.
- Icon-only buttons MUST have a descriptive `aria-label` explaining the action (e.g., `"Close settings"`, `"Open settings menu"`).
- Images MUST have meaningful `alt` text.
- Game tiles should have descriptive content via their visible word text.

### Visual Contrast
- App text and foreground content must maintain:
  - **3:1 contrast ratio** for large text (18pt+ or 14pt+ bold) and graphics
  - **4.5:1 contrast ratio** for small text (less than 18pt or less than 14pt bold)
- The neon color scheme on black background generally provides strong contrast.
- Avoid pairing low-contrast colors (e.g., light gray on white, dark gray on black) for text readability.
- Text on `bg-zinc-900` or `bg-black` backgrounds should use white or bright neon colors for sufficient contrast.

---

## Critical Rules (from project_rules.md)

1. **DO NOT change game logic or design unless explicitly told.**
2. **DO NOT add/delete CSV data unless explicitly told.**
3. **DO NOT change fonts or color schemes unless explicitly told.**
4. **When asked to "clear cache" — clear cache only. No code changes.**
5. **Keep neon color scheme.** Colors from the logo are encouraged.
6. **Header/footer must not bleed into system bars.**
7. **Timer should only appear on Mind Match level.**
8. **A game tile should never repeat on the same level, even across categories.**
9. **Tile swap animation:** First selected = neon blue + white glow; Second = neon pink + white glow. Words fade/fade after swap. Locked = yellow + white glow.
10. **Hints must respect off/on toggle.**
11. **Data source by game mode — NEVER CHANGE:**
    - **THEMED level** → pulls data from **`csvThemeData*.ts`** files ONLY
    - **CLASSIC level** → pulls data from **`csvPoolData*.ts`** files ONLY
    - **MIND MATCH level** → pulls data from **`csvPoolData*.ts`** files ONLY
    - **EXPANSION level** → pulls data from **`csvPoolData*.ts`** files ONLY
    - **EMOJI level** → pulls data from **`emojiData.ts`** ONLY
    - **SYNONYMS level** → pulls data from **`CSV_SYNONYMS_*.ts`** files ONLY
    - **HIDDEN level** → pulls data from **`csvPoolData*.ts`** files ONLY
    - **FILTER level** → pulls data from **`csvPoolData*.ts`** files ONLY
    - **GROUP level** → pulls data from **`csvPoolData*.ts`** files ONLY
    These mappings must never be altered. Each game mode has a dedicated data source.
12. **When a game tile and a category tab is long pressed the definition of the word in the game tile appears.**
13.  **Hints can only be used twice per level.  Player starts the game with 2 hints.  Rewarded hints net 2 new hints** 
---

## MCP Server Tools Available

The following MCP tools are available to assist with this project:

### Project-Specific MCP Tools (word-pairing-mcp)
- **search_game_content** - Search across all CSV data files for matching words, categories, or definitions
- **validate_manifest** - Validate metadata.json and level structure
- **analyze_level_progression** - Analyze the deterministic level sequence
- **audit_csv_consistency** - Check for duplicate entries or structural issues in CSV files
- **find_unused_categories** - Find categories in data that aren't referenced by any level

### Build Pipeline MCP Tools (build-pipeline-mcp-server)
- **bump_version** - Bump the app version in capacitor.config.json, package.json, and build.gradle
- **validate_build** - Run `npm run build` and validate output
- **validate_capacitor_config** - Validate Capacitor configuration for Android
- **check_admob_ids** - Check AdMob ad unit IDs (test vs production)
- **generate_changelog** - Generate changelog from git history
- **verify_dist** - Verify dist/ build output directory
- **full_build_audit** - Complete build and release audit
- **diagnose_theme_data** - Diagnose themed level data loading issues
- **verify_mode_names** - Verify header mode name consistency across all components (checks MODE_NAME_COLORS in Header.tsx, displayModeName in Level1_Standard.tsx, modeNames in all level components, and .clinerules documentation)

### General MCP Tools
- **github-integration** - Git status, commits, branches, pushes, changelogs
- **kanban** - Task/board management for sprint tracking
- **guesswho-game-tester** - Game simulation and bug scanning (generic use)
- **memory** - Knowledge graph for persistent context
---

## Common Tasks

### Adding new CSV content
1. Add data to the appropriate `csvPoolData*.ts` or `csvThemeData*.ts` file
2. Follow the CSV row format: `id, name, description, word1, def1, word2, def2, ..., difficulty, broadCategory`
3. Ensure no duplicate word tiles within the same level
4. Use the `audit_csv_consistency` MCP tool to validate

### Building for Android
1. `npm run build` (builds to `dist/`)
2. `npx cap copy android` (copies web assets to Android)
3. `npx cap open android` (opens Android Studio)
4.  Only build apk or aab when asked to build

### Testing levels
1. Start dev server: `npm run dev`
2. Game runs on `http://localhost:3000`
3. Level sequence is deterministic (see `DETERMINISTIC_LEVEL_SEQUENCE` in types.ts)

### Debugging
- Check `LevelPrecheckLog.txt` for level validation issues
- Use `LevelPreCheck` service warnings for CSV data problems
- Browser console for runtime errors

### Accessibility Audits
1. Verify touch target sizes: All interactive buttons must have `min-w-[48px] min-h-[48px]`
2. Verify content descriptions: Check for `aria-label` on all icon-only buttons and interactive elements
3. Check color contrast: Ensure text on dark backgrounds uses bright neon or white colors
