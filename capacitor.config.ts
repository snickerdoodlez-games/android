import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.snickerdoodlez.wordpairing',
  appName: 'Word Pairing Machine',
  webDir: 'dist'
};

export default config;
